    import XCTest
    @testable import Richie

    final class RichieTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(Richie().text, "Hello, World!")
        }
    }
